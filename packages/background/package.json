{
  "name": "background",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "tsc && vite build"
  },
  "dependencies": {
    "p-debounce": "^4.0.0"
  }
}
